"""
The match list: what a library of `.vrf` files says about itself.

`scan` is headless -- it imports no tkinter and opens no socket -- and reads
only plain chunks, so a whole library is described without an Oodle DLL.
`cards` is the CustomTkinter page built on top of it.
"""
