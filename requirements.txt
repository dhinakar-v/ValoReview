# The runtime dependencies, mirroring pyproject.toml's [project]
# dependencies for anyone installing with pip rather than uv.
# Development tools (pytest, ruff) live in the dev dependency group.
customtkinter>=6.0.0
fastapi>=0.115.0
Pillow>=11.0.0
uvicorn>=0.30.0
